package exercise.grades

import org.scalatest.FunSuite

class ExercisePackageObjectTest extends FunSuite {
	// FIXME sprawić, by test przechodził bez żadnych dodatkowych importów i dopisywania kodu w teście
	test("average value exercise test") {
		// porada: można szybko zsumować liczby w liście korzystając za pomocą metody 'sum', np. mojaLista.sum
		
//		 assert(grades.size == 3) // lista lub inna kolekcja
//		 assert(calculateFinalGrade(grades) == 4)
	}
}
