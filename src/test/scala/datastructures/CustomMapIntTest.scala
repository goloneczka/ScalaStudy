package datastructures

import org.scalatest.FunSuite

class CustomMapIntTest extends FunSuite {
	// FIXME napisać klasę CustomMap, ktora będzie niemutowalną mapą,
	//       akceptującą listę tupli z 2 typami generycznymi;
	//       niech posiada 2 metody: `size` oraz `get` zwracający wartość pod danym kluczem
	test("custom immutable map int test") {
//		val cm = new CustomMap[String, Double](
//			List(
//				("pi", 3.14),
//				("acceleration", 9.81)
//			)
//		)
//		assert(cm.size == 2)
//		assert(cm.get("pi").get == 3.14)
//		assert(cm.get("acceleration").get == 9.81)
	}
}
